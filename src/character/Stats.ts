export type Stats = {
  ac: number;
  attackBonus: number;
  damageBonus: number;
  damageMax: number;
  maxHP: number;
  monsterDamageMax: number;
};
export type Stat = "ac" | "attackBonus" | "damageBonus" | "damageMax" | "maxHP";
